{
  "version": 2,
  "framework": null,
  "installCommand": "npm install --prefix frontend --no-package-lock",
  "buildCommand": "npm run build --prefix frontend",
  "outputDirectory": "frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}